spring.application.name=app
server.port=8080

# -----------------------------------------------------------
# Oracle DataSource
# -----------------------------------------------------------
spring.datasource.url=jdbc:oracle:thin:@(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=rs03-db-inf-min.ad.fh-bielefeld.de)(PORT=1521))(CONNECT_DATA=(SID=ORCL)))
spring.datasource.username=mmittelstedt
spring.datasource.password=\!271809TmA!\
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.hikari.initialization-fail-timeout=0

# Connection Pool (HikariCP)
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# -----------------------------------------------------------
# JPA / Hibernate
# -----------------------------------------------------------
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# -----------------------------------------------------------
# JWT
# -----------------------------------------------------------
jwt.secret=moebelhaus-super-geheimer-schluessel-2024-minimum-256-bit
jwt.expiration-ms=86400000

# -----------------------------------------------------------
# CORS (für lokale Entwicklung)
# -----------------------------------------------------------
cors.allowed-origins=http://localhost:3000,http://localhost:5500

# -----------------------------------------------------------
# Logging
# -----------------------------------------------------------
logging.level.de.moebelhaus=DEBUG
logging.level.org.springframework.security=INFO